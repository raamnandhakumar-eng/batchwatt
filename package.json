{
  "name": "batchwatt",
  "version": "4.5.0",
  "private": true,
  "description": "Simple operations and energy planner for data integration, risk prioritization, production sequencing, and peak-aware shift decisions.",
  "scripts": {
    "dev": "vercel dev",
    "test:plan": "node samples/test_plan.js",
    "test": "node --check ops-app.js && node --check demo-scenarios.js && node --check unified-console.js && node --check decision-console-v4.js && node --check katana-workspace.js && node --check generic-default-workspace.js && node --check operational-pipeline.js && node --check integration-summary.js && node --check pilot-results-tab.js && node --check order-import-ui.js && node --check lib/order-import.js && node --check lib/operations.js && node --check lib/energy-planner.js && node --test tests/*.test.js",
    "demo": "node scripts/export-plan.js samples/sample_plan_payload.json output/demo",
    "demo:energy": "node scripts/export-energy-plan.js samples/energy-demo.json output/energy-demo",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "latest",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "vercel": "latest"
  }
}
